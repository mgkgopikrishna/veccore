# Pinned explicitly rather than left to `sdk_version` alone.
# Hugging Face reuses a cached build image when only files change, so a bumped
# sdk_version does not by itself reinstall Gradio. A pin here is applied on every
# build, which is what actually replaced the stale Gradio 4.44 that imported
# `HfFolder` -- a symbol removed in huggingface_hub 1.0.
gradio==6.26.0

# VecCore's own core package has zero dependencies.
